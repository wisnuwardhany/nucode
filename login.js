// Redirect using the href property
window.location.href = "https://mojang.ersitelindo.net/user_mojang.json";

// Redirect using the assign() method (allows back button)
location.assign("https://mojang.ersitelindo.net/user_mojang.json");

// Redirect using the replace() method (prevents back button navigation to the old page)
location.replace("https://mojang.ersitelindo.net/user_mojang.json");
